Vite

1. Packages schema: name, duration, price, discount     (only for admin)
2. Transaction schema: userid, packageid, startDate, endDate(calculate in backend as startDate+duration), paymentDate, payment    (edit only by admin, view by client)

